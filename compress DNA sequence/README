For ease of use, no compilation is required
If you want to perform a reordering operation, open the reordering folder
After the reordering is executed, open the compress folder, the compression process can be directly executed
