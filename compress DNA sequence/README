For ease of use, no compilation is required
If you want to perform a reordering operation, enter the reordering folder
After the reordering is executed, the compression process can be directly executed
